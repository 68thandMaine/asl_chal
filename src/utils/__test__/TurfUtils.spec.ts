import { findIntersection } from '../TurfUtils';

it("Returns null if a polyline dos not intersect a polygon", () => {

});

it("Returns a <FeatureCollection> containing coordinates of the intersect", () => {

});
